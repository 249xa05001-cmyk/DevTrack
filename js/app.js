  document.addEventListener("DOMContentLoaded", () => {

    initializeTheme();

    initializeGoals();

    initializeNotes();

    initializeStreak();

    initializeAnalytics();
    initializeDashboard();
    initializeNotifications();
}); 
